<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zsw.dao.mappers.UserRoleMapper">
    
    <resultMap id="userRoleResultMap"  type="com.zsw.pojo.role.UserRole">
        <result column="user_id" property="userId" jdbcType="VARCHAR"></result>
        <result column="role_id" property="roleId" jdbcType="NUMERIC"></result>
    </resultMap>
    
    <sql id="userRoleCol">
        user_id, role_id
    </sql>

    <select id="findByRoleId" parameterType="Integer" resultMap="userRoleResultMap">
        select <include refid="userRoleCol"/> from v_relation_user_role
        where role_id = #{id}
    </select>

    <select id="findByUserId" parameterType="String" resultMap="userRoleResultMap">
        select <include refid="userRoleCol"/> from v_relation_user_role
        where user_id = #{id}
    </select>

    <delete id="delete" >
        delete from v_relation_user_role 
        where 1 = 1
        <if test="roleId != null">
        and  role_id = #{roleId}
        </if>
        <if test="userId != null">
        and user_id = #{userId}
        </if>
    </delete>

    <insert id="insert" >
        insert into v_relation_user_role (role_id, user_id) values(#{roleId}, #{userId})
    </insert>
    



</mapper>