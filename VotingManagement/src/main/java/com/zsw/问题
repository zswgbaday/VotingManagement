1.【已解决】关于权限管理和302状态码
    302状态码在百度上的意思是：该资源已经移动或不可用，同时会返回一个该资源的新位置或者替代资源的位置。
    我这里遇到的情况是：在shiro权限管理中，由于我拦截了所有请求，但对static开头的url没有放开权限，所以导致静态文件不可读，响应了302.
    解决办法】
    改了权限拦截和springmvc拦截为一个url，以api开头的所有请求拦截，其他请求一律认为是请求静态资源。
2.【未解决】我查到的关于web.xml中的匹配规则是有四种
    1.精确路径匹配
    2.最长路径匹配
    3.拓展匹配
    4.以/开头，/*结尾的路径匹配。但是路径匹配我尝试后发现是失败的